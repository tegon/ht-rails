<h1>Últimas perguntas</h1>

<dl class="filters">
  <dt>Filter posts</dt>

  <dd><%= link_to t("filter.all"), questions_path %></dd>
  <dd><%= link_to t("filter.unanswered"), questions_path(:filter => QuestionFilter::UNANSWERED) %></dd>
</dl>

<%= render @questions %>
<%= paginate @questions %>

<% content_for :sidebar do %>
  <%= render "categories", :categories => @sidebar.categories %>
  <%= render "users", :users => @sidebar.users %>
<% end %>